package com.damedesuyo.irc.god_bot.Projects;

public class EpisodeWorkflow {
	private Task[] tasks;
	
	//-------------------------------
	// Constructors
	public EpisodeWorkflow()
	{
		
	}
	
	//-------------------------------
	// Public Methods
	/**
	 * @return <Task> Current task if available. Null if all tasks in workflow complete.
	 */
	/*
	Task getCurrentTask()
	{
		for(Task task : this.tasks)
		{
			if(task.subtasks != null)
			{
				if(task.areSubtasksComplete())
				{
					
				}
			}
			else if(!task.done)
			{
				return task;
			}
		}
	}
	*/
	
	
	//-------------------------------
	// Private Methods
}
